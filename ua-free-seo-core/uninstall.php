<?php
/**
 * UA FREE SEO Core intentionally keeps settings and metadata on uninstall.
 * Destructive cleanup is available only through a separately confirmed migration workflow.
 */
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
