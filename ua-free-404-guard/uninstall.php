<?php
/**
 * Data is intentionally preserved when the plugin is removed.
 * Destructive cleanup must be an explicit, separately confirmed action.
 */
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
