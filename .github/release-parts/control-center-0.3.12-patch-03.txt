--- cc-old/ua-free-analytics-dashboard/includes/class-uafree-analytics-dashboard.php	2026-07-31 14:46:58.387151296 +0000
+++ cc-new/ua-free-analytics-dashboard/includes/class-uafree-analytics-dashboard.php	2026-07-31 14:46:58.387151296 +0000
@@ -137,7 +137,6 @@
 	);
 
 	public static function init(): void {
-		add_action( 'plugins_loaded', array( __CLASS__, 'load_textdomain' ) );
 		add_action( 'admin_menu', array( __CLASS__, 'register_menu' ), 35 );
 		add_action( 'admin_menu', array( __CLASS__, 'replace_suite_root_menu' ), 10000 );
 		add_action( 'admin_enqueue_scripts', array( __CLASS__, 'enqueue_assets' ) );
@@ -209,13 +208,6 @@
 		}
 	}
 
-	public static function load_textdomain(): void {
-		load_plugin_textdomain(
-			'ua-free-analytics-dashboard',
-			false,
-			dirname( plugin_basename( UAFREE_ANALYTICS_DASHBOARD_FILE ) ) . '/languages'
-		);
-	}
 
 	public static function register_menu(): void {
 		add_submenu_page(
@@ -400,10 +392,14 @@
 	}
 
 	private static function current_is_uafree_page(): bool {
-		$page = isset( $_GET['page'] ) ? sanitize_key( wp_unslash( $_GET['page'] ) ) : '';
-		return 'uafree-suite' === $page
-			|| str_starts_with( $page, 'uafree-' )
-			|| str_starts_with( $page, 'ua-free-' );
+		$screen = get_current_screen();
+		if ( ! $screen ) {
+			return false;
+		}
+
+		$screen_id = sanitize_key( (string) $screen->id );
+		return str_contains( $screen_id, 'uafree' )
+			|| str_contains( $screen_id, 'ua-free' );
 	}
 
 	public static function render_global_support(): void {
@@ -1287,9 +1283,15 @@
 			);
 		}
 
-		$days = isset( $_GET['days'] )
-			? absint( wp_unslash( $_GET['days'] ) )
-			: 30;
+		$days = 30;
+		if ( isset( $_GET['days'], $_GET['_wpnonce'] ) ) {
+			$period_nonce = sanitize_text_field(
+				wp_unslash( (string) $_GET['_wpnonce'] )
+			);
+			if ( wp_verify_nonce( $period_nonce, 'uafree_control_center_period' ) ) {
+				$days = absint( wp_unslash( $_GET['days'] ) );
+			}
+		}
 		$days = self::normalize_days( $days );
 		$summary = self::control_summary( $days );
 		$report = (array) ( $summary['report'] ?? array() );
@@ -1338,14 +1340,18 @@
 			'</strong>';
 		foreach ( self::PERIODS as $period ) {
 			$class = $period === $days ? 'button button-primary' : 'button';
-			$url = add_query_arg(
-				array( 'page' => self::PAGE, 'days' => $period ),
-				admin_url( 'admin.php' )
+			$url = wp_nonce_url(
+				add_query_arg(
+					array( 'page' => self::PAGE, 'days' => $period ),
+					admin_url( 'admin.php' )
+				),
+				'uafree_control_center_period'
 			);
 			echo '<a class="' . esc_attr( $class ) . '" href="' .
 				esc_url( $url ) . '">' .
 				esc_html(
 					sprintf(
+						/* translators: %d: number of days in the report period. */
 						__( '%d days', 'ua-free-analytics-dashboard' ),
 						$period
 					)
@@ -1483,9 +1489,15 @@
 		}
 		check_admin_referer( self::EXPORT_ACTION );
 
-		$days = isset( $_GET['days'] )
-			? absint( wp_unslash( $_GET['days'] ) )
-			: 30;
+		$days = 30;
+		if ( isset( $_GET['days'], $_GET['_wpnonce'] ) ) {
+			$period_nonce = sanitize_text_field(
+				wp_unslash( (string) $_GET['_wpnonce'] )
+			);
+			if ( wp_verify_nonce( $period_nonce, 'uafree_control_center_period' ) ) {
+				$days = absint( wp_unslash( $_GET['days'] ) );
+			}
+		}
 		$days = self::normalize_days( $days );
 		$report = self::report( $days );
 
diff -ruN cc-old/ua-free-analytics-dashboard/readme.txt cc-new/ua-free-analytics-dashboard/readme.txt
--- cc-old/ua-free-analytics-dashboard/readme.txt	2026-07-31 14:46:58.387151296 +0000
+++ cc-new/ua-free-analytics-dashboard/readme.txt	2026-07-31 14:46:58.387151296 +0000
@@ -3,8 +3,8 @@
 Donate link: https://uafree.org/plugins/support-development/
 Tags: dashboard, diagnostics, plugin suite, status, management
 Requires at least: 6.2
